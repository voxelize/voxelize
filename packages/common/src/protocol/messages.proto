syntax = "proto3";
package protocol;

message Message {
  enum Type {
    INIT = 0;
    JOIN = 1;
    SIGNAL = 2;
    ECHO = 3;
    ERROR = 4;
    LEAVE = 5;
    PEER = 6;
  }
  
  Type type = 1;
  string json = 2;
  string text = 3;

  repeated string peers = 4;
}