syntax = "proto3";
package protocol;

message Message {
  enum Type {
    ECHO = 0;
    ERROR = 1;
    INIT = 2;
    JOIN = 3;
    LEAVE = 4;
    LOAD = 5;
    PEER = 6;
  }
  
  Type type = 1;
  string json = 2;
  string text = 3;
}