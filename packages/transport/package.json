{
  "name": "@voxelize/transport",
  "version": "1.0.33",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    "require": "./dist/index.js",
    "import": "./dist/index.mjs",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "watch": "npm-run-all --parallel \"compile -w\" \"types -w --preserveWatchOutput\"",
    "clean": "rimraf ./dist",
    "compile": "vite build",
    "build": "yarn run clean && yarn run compile && yarn run types",
    "types": "tsc --emitDeclarationOnly --outDir ./dist -p ./tsconfig.json --declaration --allowJs",
    "prepublishOnly": "yarn run build",
    "demo": "node example/index.js",
    "preinstall": "npx only-allow pnpm"
  },
  "dependencies": {
    "@voxelize/protocol": "workspace:*",
    "fflate": "^0.7.3",
    "protobufjs": "^7.2.2",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "@types/protobufjs": "^6.0.0",
    "@types/websocket": "^1.0.5",
    "protobufjs-cli": "^1.1.1"
  }
}
