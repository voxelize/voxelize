{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check:wasm": "node ./scripts/check-wasm-mesher.mjs",
    "check:wasm:json": "node ./scripts/check-wasm-mesher.mjs --json",
    "check:wasm:json:compact": "node ./scripts/check-wasm-mesher.mjs --json --compact",
    "check:wasm:verify": "node ./scripts/check-wasm-mesher.mjs --verify",
    "check:wasm:verify:json": "node ./scripts/check-wasm-mesher.mjs --json --verify",
    "check:wasm:verify:json:compact": "node ./scripts/check-wasm-mesher.mjs --json --verify --compact",
    "typecheck": "tsc --noEmit",
    "demo": "pnpm run check:wasm && vite --clearScreen false --port 3000",
    "build": "pnpm run check:wasm && pnpm run typecheck && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/p5": "^1.7.6",
    "typescript": "^5.4.5",
    "vite": "^4.5.9"
  },
  "dependencies": {
    "@types/three": "^0.165.0",
    "@voxelize/aabb": "^1.0.0",
    "@voxelize/core": "workspace:*",
    "lil-gui": "^0.17.0",
    "p5": "^1.11.3",
    "postprocessing": "^6.37.1",
    "three": "^0.165.0"
  }
}
