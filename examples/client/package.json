{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "demo": "vite --clearScreen=false --port=3000",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@voxelize/core": "*",
    "@voxelize/network": "*",
    "@voxelize/protocol": "*",
    "three": "^0.156.0"
  },
  "devDependencies": {
    "@types/three": "^0.155.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
