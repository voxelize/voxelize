{
  "name": "@voxelize/client",
  "version": "1.3.95",
  "license": "MIT",
  "main": "dist/index.cjs.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "umd": "dist/voxelize.min.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel \"compile -w\" \"types -w --preserveWatchOutput\"",
    "clean": "rimraf ./dist",
    "compile": "rollup -c rollup.config.js",
    "build": "yarn run clean && yarn run compile && yarn run types",
    "types": "tsc --emitDeclarationOnly --outDir ./dist -p ./tsconfig.json --declaration --allowJs",
    "prepublishOnly": "yarn run build",
    "docs": "yarn run docs:start",
    "docs:start": "cd docs && rimraf docs/docs/api && yarn && yarn run start",
    "docs:build": "cd docs && yarn && yarn run build",
    "docs:deploy": "cd docs && yarn && yarn run deploy"
  },
  "peerDependencies": {
    "postprocessing": "^6.26.3",
    "three": "^0.146.0",
    "three-nebula": "10.0.3"
  },
  "devDependencies": {
    "@types/gl-vec3": "^1.1.1",
    "@types/ndarray": "^1.0.11",
    "@types/omggif": "^1.0.3",
    "@types/three": "^0.141.0",
    "@types/uuid": "^8.3.4",
    "postprocessing": "^6.29.0"
  },
  "dependencies": {
    "@tweenjs/tween.js": "^18.6.4",
    "@voxelize/aabb": "^1.0.0",
    "@voxelize/physics-engine": "^1.1.26",
    "@voxelize/raycast": "^1.0.8",
    "@voxelize/transport": "*",
    "deepmerge": "^4.2.2",
    "domurl": "^2.3.4",
    "events": "^3.3.0",
    "fflate": "^0.7.3",
    "gl-vec3": "^1.1.3",
    "ndarray": "^1.0.19",
    "omggif": "^1.0.10",
    "regenerator-runtime": "^0.13.9",
    "socket.io-client": "^4.4.1",
    "uuid": "^8.3.2"
  }
}
