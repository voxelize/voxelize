#![feature(thin_box)]

mod bus;
mod event;
mod handler;
mod payload;
mod storage;
mod system;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
